name: CI

on:
  push:
  pull_request:

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Run capability test suite
        run: python3 -m unittest discover -s .gsd/capabilities/markdown-linting/tests -v
